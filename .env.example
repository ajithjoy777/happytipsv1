# Copy this file to .env before running anything (npm run db:seed / npm run dev).
DATABASE_URL="file:./dev.db"

# Used to build absolute URLs for the guest tip QR codes. Set this to your
# real deployed URL in production (e.g. https://app.happytips.com).
NEXT_PUBLIC_SITE_URL="http://localhost:3000"
